# Plan: landing-page-gen

## Milestones

| # | Milestone | Exit criterion | Status |
|---|---|---|---|
| 0 | Environment: repo, CLIs, hooks, skills, agents, docs | `uv run pytest` passes; `lp-corpus --help`, `lp-inject --help` print; skill and agents load; hook smoke test passes | done 2026-09-04; deferred: live hook check needs a fresh session (hooks load at start), `.env.example` blocked by the workspace deny rule |
| 1 | Corpus: discover pages, fetch, sectionize, DB, `similar`, `skeleton` | pytest on a saved HTML fixture; `similar --type hero` returns hero sections from other pages with media; unquoted step costs measured | done 2026-09-04: 19 tests on `tests/fixtures/streamed.html`; `similar` returns one hero per page with PNG media; extend/edit/reframe quoted in `tool-map.md`; corpus = 199 pages: the 205 non-hub paths in `pages.yaml` minus 6 that serve the client-side app shell (`/ai-agent/`, `/ai-avatar/`, `/ai-enhance/`, `/ai-models/midjourney/`, `/ai-storyline/`, `/batch/`; recorded as `shell` in meta.json), built by `lp-corpus fetch --family ... --sectionize` and `lp-corpus media --all` |
| 2 | Dry run of the loop | `--dry-run` produces priced `workflow.yaml` per slot, reviewer critiques, `lp-inject` fills placeholders, ledger has zero paid rows | done 2026-09-04 on `runs/dry-1` (resize-image, 6 sections, 14 slots): every slot has a preflighted `workflow.yaml`, each section reviewed (5 of 6 needed one rework round), `dist/index.html` renders with 14 labelled placeholders, ledger has 0 paid rows; the manager procedure was run by the main session step by step, not via `/build-landing-page` |
| 3 | Live run, images only, ≤ 200 credits | every creative/thumbnail slot accepted or blocked with a reason; every brief names a style family and composite slots go through `lp-compose`; ai-template-generator S07 re-run reads as `template-mockup`; `dist/index.html` renders; spend matches ledger and `picsart_credits` delta | todo | 2026-09-08: composite check exercised outside the worker loop by `runs/trial-4` (`panel-overlay` at 4:3 and 5:4, tilted variant); a worker-driven composite slot is still to run |
| 4 | Video and tuning | hero video via still-to-motion; rework loop tuned; rules in `prd.md` | todo |

Page inventory: `corpus/pages.yaml` (272 paths; families: tool 99, ai-models 87,
other 41, compare-models 18, hub 13, ai-tool 14). Regenerate with
`uv run lp-corpus discover --seed <url>`; the 2026-09-08 pass added 54 pages
from a depth-3 crawl that also expands `other` pages. The corpus holds every
family including `hub` and the home page (`home`); 10 inventory paths are
client-side app shells and have no snapshot. 262 pages, 3772 sections,
9255 media are indexed.
Snapshots live in `corpus/pages/<slug>/` (raw.html, page.html with `data-lp*`
stamps and local `media/` links, media/, page.png, render.json, meta.json,
sections.md); only sections.md and meta.json are committed. The 199
snapshots take 4 GB on disk (media, full-page screenshots, raw HTML); 9 of
4551 media nodes point at assets the CDN no longer serves (404) and keep
their absolute URL.

## Decisions log

| Date | Decision | Why |
|---|---|---|
| 2026-09-04 | Corpus comes from scraping live picsart.com pages with Playwright. | Pages are CMS-driven and client-rendered; no internal export is assumed. |
| 2026-09-04 | Output is re-injection into the scraped HTML snapshot, not a template render. | Highest fidelity; every new page starts from a snapshot of an existing one. |
| 2026-09-04 | Manager is the main session; workers and reviewers are subagents. | Subagents cannot spawn subagents; SendMessage keeps a worker's context across rework rounds. |
| 2026-09-04 | Workers author `workflow.yaml` before executing; reviewers critique steps, and rework re-runs from step N. | Reuses upstream URLs; makes prompts and model choices reviewable, not just the final pixel. |
| 2026-09-04 | Defaults: `gemini-3-pro-image` (Nano Banana Pro) for images, `seedance-2.5` for video, `seedance-2.0-mini` as video draft tier. | Areg's brief; quoted costs in the `picsart-workflows` skill tool map. |
| 2026-09-04 | Paid calls only via connector `b05f6314`, only after a preflight, only inside an active run; enforced by `hooks/credit_guard.py`. | Wrong-connector and unquoted spend are the two mistakes worth making impossible. |
| 2026-09-04 | Roles `ui-screenshot`, `icon`, `decorative` are kept from source. | AI cannot faithfully render product UI; icons are brand assets. |
| 2026-09-04 | Page discovery is a plain-HTTP link crawl from hub pages (`discover.py`); no sitemap exists (`/sitemap.xml` returns the SPA shell). Pages are server-rendered (1.4 MB HTML with links), so Playwright is a fallback for `fetch`, not the default. | Faster, no browser; verify per page that sections are in the static HTML. |
| 2026-09-04 | Hooks run on system `python3`, stdlib only. | Fast start, no dependency on the project venv. |
| 2026-09-04 | Dry run findings: workers plan well but copy the hero's composition into every section unless told that anchored means light and palette only; every first-round review asked for prompt changes, none for pattern changes; the rework loop via SendMessage worked (worker context intact, one round each); small thumbnails go to `gemini-3.1-flash-image` at a native ratio (3 credits) instead of the pro model cropped. | Lessons are in `prd.md`; the brief template should carry the anchored rule so reviewers stop repeating it. |
| 2026-09-04 | `check_result.py` takes the section from the first `sections/Sxx` path in the worker transcript (its assignment), not the last. | In the dry run a worker that listed the run folder was blocked on stop for another worker's section (S06 asked to finish S11). |
| 2026-09-04 | Skills, agents and hook wiring moved from the workspace `.claude/` into this repo's `.claude/`; hook commands are cwd-relative (`python3 hooks/...`) and `_ledger.current_run()` resolves `runs/current` from the hook file's own location. | Claude Code loaded this repo's `.claude/settings.json` (hooks fired live), but `$CLAUDE_PROJECT_DIR` resolves to the workspace folder, so anything path-dependent must not use it. The workspace copies had no version control. |
| 2026-09-04 | `fetch` gets the HTML by plain HTTP and reassembles the React streaming segments in Python (`<template id="P:x">` and Suspense `B:x` boundaries swapped for the hidden `<div id="S:x">` chunks, as the inline `$RC` scripts would). Playwright still runs once per page, for the full-page screenshot and the rendered box of every image and video. | The reassembled `<main>` was checked identical to the rendered DOM (31 images, 5 videos on `/ai-image-generator/`); slot sizes only exist after layout, so geometry comes from the browser. |
| 2026-09-04 | Before the full-page screenshot, inject `*{content-visibility:visible!important}`. | Section wrappers use `content-visibility:auto`; without the override everything below the fold is captured blank. `networkidle` never fires on these pages (analytics), so `load` + a scroll pass is used. |
| 2026-09-04 | A section is one non-empty direct child of `<main>`, plus the body-level `<aside>` (link chips) and `<footer>`. Type comes from the CMS component labels (`data-testid`, `data-pulse-section-group`: banner-block, promotional-component, how-it-works-section, use-cases, tutorials-section, faq-section, pricing-cards, ...) and falls back to structural heuristics (headings, media count, tabs, card links). | Labels are stable across pages and free; heuristics alone confused captioned galleries with link grids. |
| 2026-09-04 | Media roles are guessed: footer/header or ≤120 px → `icon`; card sections → `thumbnail`; alt words decoration/badge/logo → `decorative`; videos in how-it-works or "inside Picsart"/"built-in tools" callouts → `ui-screenshot`; else `creative`. Media with a 0×0 rendered box is dropped. | A wrong `creative` would spend credits on a UI shot; a wrong `ui-screenshot` only loses a slot. The skeleton is hand-edited before a run. |
| 2026-09-04 | `sectionize` stamps the snapshot: `data-lp-section="S03"`, `data-lp="S03-m1"`, `data-lp-t="S03-t2"`; the DB stores those selectors and `skeleton` emits `slots.json` with them. | `lp-inject` then needs no DOM-path selectors; the stamps survive any re-serialisation of `page.html`. |
| 2026-09-04 | Media `src` is stored as the CDN asset URL (Next.js `/_next/image?url=` proxy unwrapped). `similar` writes examples as PNG: AVIF/WebP converted with Pillow, videos as a still grabbed with Chromium at t=1 s. | Agents view files with `Read`, which cannot open AVIF or WebM; Playwright's bundled ffmpeg has no VP9 decoder. |
| 2026-09-04 | `skeleton.md` format: YAML frontmatter (`page, source, snapshot, brand, audience, defaults, budget, notes`), `## Sxx type`, `- tN tag: text -> href`, one fenced ```slot YAML block per media node (`id, kind, role, size, aspect, natural, duration_s, src, alt`), `> annotation:` per generated-role slot. `aspect` is quoted because bare `9:16` is a sexagesimal integer to YAML 1.1 parsers. | One file the manager parses and a human edits; ids map back to the stamps. |
| 2026-09-04 | Snapshots hold their media: `fetch` (and `lp-corpus media` for older snapshots) downloads every `img src`, `video src` and `poster` into `corpus/pages/<slug>/media/<stem>-<8 hex of the URL><ext>`, points the attribute at the copy, keeps the served attribute in `data-lp-src` / `data-lp-poster`, drops `srcset`, `sizes`, image preloads and `<picture>` sources, and removes `<base>` after absolutising the remaining relative links. `media.local_path`, `slots.json` `local`, the `skeleton.md` slot block `local:` and the `sections.md` media link (CDN URL as link title) all name the copy; `similar` copies from it. Stylesheets, fonts and CSS backgrounds stay remote. | Areg's call: a snapshot must not lose its images when the CDN drops an asset, and `lp-inject` can ship `media/` with `dist/`. Relative `media/` paths cannot coexist with `<base href="https://picsart.com/">`. |
| 2026-09-04 | `similar`: BM25 over section Markdown filtered by type, one section per page, source page excluded with `--exclude`, only sections with a `creative`/`thumbnail` slot, type-only fallback when the query matches nothing; at most 4 media files per example. | Briefs need media to look at, not the page's own section; 10 hero carousel images per example bloated a brief to 15 MB. |
| 2026-09-04 | Package uses a `src/` layout with one package and two console scripts. | Zero build-backend configuration with `uv_build`. |
| 2026-09-04 | Trial `runs/trial-1`: one random creative slot (ai-template-generator S07, 480x480) produced from the section text alone, source image, alt and paths withheld from the worker; 20 credits, one gate failure (a real brand mark inside a generated inset photo), balance delta matched the ledger. | Checks whether the brief carries enough for a worker without the original; the result is on-brief but generic (nature-photo cards) where the original is a bold branded template with tool icons, so the annotation, not the copy, decides the look. |
| 2026-09-04 | Repo declares an empty `[tool.uv.workspace]`. | `~/pyproject.toml` is a uv workspace; without this, uv adopts the project as a member and puts `.venv` and `uv.lock` in the home directory. |
| 2026-09-07 | Seven style families derived from contact sheets of the corpus creatives (`dark-composite`, `before-after`, `crop-frame`, `cutout-checkerboard`, `template-mockup`, `prompt-card`, `full-bleed`), documented in one skill file `picsart-workflows/style-families.md` with a fixed nine-key block per family; the manager copies one block into the brief. | Trial-1 showed the annotation alone yields a generic image; every real callout is a composite card with UI-like chrome, which no prompt rule described. One file: one `Read` for the manager, shared vocabulary once. Code says `style` (`> style:`, `media.style`, `similar --style`) because `family` already means the page family. |
| 2026-09-07 | Chrome (tiles, pills, brackets, checkerboard, badges, template card) is drawn locally by `lp-compose` (Pillow, templates as Python dicts in `compose/families.py`, geometry at 1600 px scaled to the slot's natural size, 2x supersampling, bundled OFL Manrope variable font, icons as 24-unit polylines). Workers generate only the photographic panels; a composite's `chosen:` is a run-relative path that `lp-inject` resolves against the run. | The no-text/no-UI rule stays intact for the model while the house style becomes reachable. Dicts over YAML: five templates, no loader. Corpus SVGs are store badges, not tool icons; Picsart's font is not recoverable from the snapshots. `picsart_media_upload` is a human drag-and-drop widget, so a local file cannot become a URL. |
| 2026-09-07 | Corpus creatives are tagged with a family by `lp-corpus styles`: Claude vision (`claude-opus-5`, structured output, ~884 hero/callout/use-case/how-it-works images, about $3), `alt` containing "before" tagged for free; persisted in `corpus/styles.yaml` keyed by CDN src and re-applied to `media.style` after every `sectionize`. `similar --style` prefers tagged sections and falls back to the untagged passes. Adds the `anthropic` dependency. | Role, size and type cannot separate the families (every candidate is a 480 px callout creative); a manual list of 30 would leave the preference inert. The yaml survives re-indexing; the SDK is the documented way to call the API. |
| 2026-09-07 | `template-mockup`'s headline box is chrome drawn by `lp-compose` (optional placeholder text or blank bars); the worker generates the template's photo only. | It is the one family whose real artefact carries readable text; drawing it keeps "images carry no text" true for the model. |
| 2026-09-07 | Trial `runs/trial-3`: one random creative slot (poster-maker S09, 480x480, `template-mockup`) produced blind, same prompt on `gemini-3-pro-image` (10 credits with one re-run) and on Gemini Omni (`gemini-omni-1.1-flash-preview`, 9 credits). Omni and Kling Omni are text-to-video only in the Picsart catalog, so the Omni panel is one frame of a 3 s static 9:16 clip grabbed with `FrameGrabber` (Playwright Chromium decodes the mp4). | Areg asked for the omni model beside the default; there is no omni image model. The worker preferred the Omni frame (single subject, even margin); the frame path costs an extra step (hand-computed crop anchor) and gives no `imageUrls` anchoring, so it stays a comparison, not a default. |
| 2026-09-07 | Blind-trial examples are picked by hand when `similar` returns sibling pages: motivation-/movie-/wanted-poster-maker serve poster-maker's CMS block and image. `check_result.py` matched `S07` from the contract example embedded in the brief and blocked the S09 worker on stop; `log_generation.py` logs async video calls with no URL (the clip URL comes from `picsart_job_status`). | Found in trial-3; fixes are open (exclude examples by asset id; take the section from the assignment, not the first path; hook `picsart_job_status` or write the URL from `workflow.yaml`). |
| 2026-09-07 | Picture text is generated. The skeleton gets a `> text:` line per generated slot (exact strings or `none`); the manager fills it from the section copy under the family's **Text** line; the brief carries a `## Text in image` table; the worker quotes the strings verbatim, ends prompts with ", no other text, no logos or watermarks", and gates spelling; the rubric gains a `text` key that is a hard reject below 5. Chrome text bound to compose geometry (pills, size labels, chips, buttons) stays with `lp-compose`; `template-mockup`'s headline moves to the model and the compose spec omits the headline item. | Areg's call after trial-3: the real poster-maker image carries "50% OFF" and "BUY NOW!" as artwork, which blank bars cannot stand in for. The manager, not the worker, decides the words so the image never invents copy and never duplicates the HTML next to it. |
| 2026-09-07 | Categorisation is derived from attributes: `lp-corpus attrs` describes every distinct generated-role asset once (ground, layout, panels, chrome, text, mockup, subject, finish, plus a family hint) into `corpus/attributes.yaml`; `taxonomy.family_of` is a deterministic rule table from attributes to `family[/ground]`; `styles.yaml` is derived from it (`styles --from-attrs`, manual entries kept); `lp-corpus taxonomy` writes the cross-tab and contact sheets; `media.attrs`, `media.style` and role fixes are re-applied after every `sectionize`. Slot classes (`<type>-<aspect class>[-video]`, `sectionize.aspect_class` / `size_class`) are the grouping key and the manager's lookup. | The seven families came from 54 hand-viewed images; the classifier saw only three doc lines per family and its confidence was never used. Attributes make the families auditable (counts per cluster), re-derivable without another model call, and let `similar` filter on ground or layout. Ground is the only variant axis this round. |
| 2026-09-07 | No vision API anywhere in the corpus pipeline. `lp-corpus attrs` measures what the pixels say (`corpus/measure.py`, numpy: ground from the border ring's median colour, checkerboard from block alternation on the top and left strips, gradient from a planar fit, panels and layout from background gutters, a full-height centre divider for `split` and, with a high row-profile correlation, `before_after`; `alt` containing "before" counts too) and writes partial records; `lp-corpus sheets` lays the assets that still lack a semantic field on numbered 12-cell PNG sheets grouped by (type, ground, layout) with a yaml manifest per sheet; a labeller (the `/label-corpus` skill fans out one subagent per sheet, or a person) writes `<sheet>.answers.yaml` keyed by cell number; `lp-corpus labels` validates every value against the enums, drops what is outside them and merges. `styles.py` keeps only load/save/apply/guide/derive; the `anthropic` dependency is gone, `numpy` is added. | Areg's call: the tagging pass wanted an API key and about $21, and the same eye is available for free in the session. The split is honest — pixels answer ground/layout/panels/before-after for 1496 assets in 87 s and place 55 % of them in a family with no judgement at all; only chrome, text, mockup, subject and finish need someone to look. Sheets are the unit because 12 assets of one measured group cost one `Read` and one short answer, and the manifest keeps the answers auditable per asset. Measured fields sit at the tail of `family_of`, so a labelled chrome or mockup still overrides the free guess. A pass writes only the records it measured (`merge_save`) and a re-measure keeps every answered field (`keep_answers`), because a long video pass otherwise clobbers a labelling merge that lands while it runs — observed once, five answers lost. Sheets are named `<type>_<ground>_<layout>-<6 hex of their assets>`, so a rebuilt sheet of different pictures can never inherit an answers file. |
| 2026-09-07 | Fourteen families: the seven kept and unchanged in id and order, plus `vs-two-up`, `mockup-card`, `cinematic-still`, `graphic-collage`, `outcome-tile`, `editor-canvas`, `model-card`. Untemplated families carry a **Template** line naming the fallback (`none; brief as X`), no chrome, or `kept-from-source` (editor-canvas and model-card thumbnails, which mock the Picsart editor). `dark-composite/light` and `template-mockup/black` are ground variants, not families. | Splitting on ground would double the compose templates for one fill colour; kept-from-source keeps the thumbnail tiles in the counts and in `similar` (their role stays `thumbnail`). Mockup cards are generated assets of their own family, not screenshots, per Areg. |
| 2026-09-08 | Coverage audit against the live site: every `img`/`video` served in a snapshot is compared with `media.src` (next/image proxies unwrapped). Three gaps found and closed: (1) two pages (`product-ad-maker`, `social-media-ad-maker`) had zero media because the site renders their content twice and the first copy measures 0×0 — `build_section` now takes the displayed copy's geometry for the same src instead of dropping the node (this also admits the five footer badges on every page, as icons); (2) 54 pages were never discovered because depth 2 stops at tool pages and `other` pages are not expanded; (3) 9 template-maker pages had swapped their thumbnails since 2026-09-04 (a content update, two live fetches agree) and were re-fetched. Hubs and the home page are now in the corpus. What is still not indexed, by design: media hidden at desktop width (93 distinct srcs, mostly the third-party model logos of the "works with" strip), CSS `background-image` assets (2 on `ad-maker`), inline SVG. | A corpus that misses a whole page's creatives skews `similar`; the drift check is cheap (plain HTTP, ~200 pages in 2 min) and worth repeating before a live run. |
| 2026-09-08 | First full-page live run `runs/live-1` on a random page (`hsl-color`, family `other`): 5 generated slots (hero 4:3 + four use-case cards briefed as a `full-bleed` Series), 21 kept, 0 blocked, 50 credits, balance delta equal to the ledger, `dist/index.html` renders. Blind: briefs carry no `src`/`local`/`alt`, `similar` excludes the page and its asset ids, the manager opens the originals only after the last verdict. The manager relabelled the "Featured In" gallery `decorative`. | Milestone 3's loop ran end to end with the skill's procedure (manager in the main session, workers and reviewers as subagents, SendMessage rework). Findings: `check_result.py` blocks every stop with a stale `S09` (still open), the `count: 1` rule never reached the worker, an `imageUrls` anchor leaked the hero's subject; rules in `prd.md`. Not yet met from the Milestone 3 criterion: no composite family exercised (the random page had only full-bleed slots) and no template-mockup re-run. |
| 2026-09-08 | Fifteenth family `panel-overlay` (a photo filling the slot with the tool's dark adjustment panel over it; heroes carry a round tool badge and a label pill instead; `/white` = the tilted stacked-card variant), with an `lp-compose` template that accepts 4:3 and 5:4, new chrome kinds `adjust-panel` and `tool-pill`, a `ground: tilted` spec option, the `adjust-panel` chrome attribute, and a rule in `family_of`. Derived from `research/live-1-style-gap/`: all five hsl-color originals are stock photos (the hero is Pexels 2180474 by R. Fera) with this chrome, which the measurer reads as `photo-full-bleed / single` and the unanswered sheets left as `full-bleed`. Checked in `runs/trial-4` (15 credits, three `gemini-3-pro-image` renders of the stock look composed with the template). Label sheets rebuilt without `--skip-resolved` (299 sheets) so resolved-but-unlabelled assets get a sheet; the three sheets holding hsl-color are answered for its cells. | The live-1 gap was structural: no family described "photo + floating editor panel", so the workers produced on-copy editorial photos with no chrome. The same chrome recurs on hair-color-changer and ai-filters, and Fotor's and VSCO's HSL pages use the same device, so it is a category convention, not a one-page quirk. |
| 2026-09-08 | Every style family gets external references of its photography: `/collect-references` spawns one `reference-collector` agent per family (Pexels and Unsplash only, every URL verified on its page, no downloads, no paid calls) writing `corpus/references/<family>.yaml` (genre, search terms, 5 to 10 examples with creator and licence, creators, what is Picsart-specific, prompt guidance, open items); `apply.py` validates and writes a **References:** line into each family block; a test pins the files to `db.STYLES` and the schema. First pass done for all 15 families (9 to 10 examples each, 3 to 6 creators). Wave 2 (`--pages`, one agent per page: which family each creative really is, chrome present or absent, photo sources) is defined but not run. | Areg's ask after trial-5: the corpus examples show layout, not where the photography comes from; hsl-color's images were Pexels stock, and a worker prompting the stock genre lands the look. Pexels and Unsplash because their licences allow the comparison and their pages name the creator. Finding across the pass: Unsplash's matching shots are mostly Unsplash+ premium, so Pexels carries most references. |
| 2026-09-08 | Run loop trimmed for tokens and wall time: workers and reviewers no longer open `style-families.md` (the brief carries the family block), briefs get 2 examples at one 480 px image each (`similar -k 2`, `--media-per-example`, `EXAMPLE_MAX_PX`) plus a `## References` section from `corpus/references/<family>.yaml`; the direct pattern is `count: 1` at 1K (2K only above 1000 px natural width); anchoring is by words from a `shared-context.md` written before the wave, so all sections spawn at once; a `precheck.py` paperwork check replaces record-only review rounds and one reviewer spawn covers the whole wave; `check_result.py` takes the section from the assignment sentence (else the most-named existing folder); `report.md` gains an "Agents" table (tokens, minutes per spawn). | Areg asked for every possible optimisation. live-1's ledger shows the paid calls took about 4 of 20 minutes; the rest was sequential waves and per-section review rounds, and each agent spawn re-read about 16k tokens of skill text plus a dozen example images. The stale-section stop bug burned a retry on every worker. To be measured on the next live run against live-1 (50 credits, 8 generates, 2 review rounds per section). |
| 2026-09-08 | Second full-page live run `runs/live-2` on a random page (`ai-models--recraft-v4-styles-pro-vector`): 16 generated slots (hero, 8 gallery tiles, 3 callouts, 4 tutorial thumbnails), 20 kept, 0 blocked, 112 credits, balance delta equal to the ledger, `dist/index.html` renders; all six sections spawned in one wave, precheck caught two record problems without a review round, one reviewer spawn per round (the first hit its 30-turn limit after four sections and was resumed). Every vector slot's annotation overrode the family's photograph wording with a flat-vector finish and a shared palette written from the skeleton. | Milestone 3's loop with the trimmed run procedure: 34 minutes for 16 slots against live-1's 20 for 5, worker-bound (the S03 Series gated 18 flash calls serially). Against the originals: the four callout/hero slots are black `dark-composite` cards with a mark tile and an SVG chip that the measurer had tagged `full-bleed` (unanswered sheets, same on the sibling pages), and the gallery originals fill the tile on saturated grounds where the brief asked for margin on off-white; rules in `prd.md`. Still not met from Milestone 3: no composite family exercised by a worker, no template-mockup re-run. Reviewer `maxTurns: 30` covers about four sections per spawn. |
| 2026-09-09 | Post-live-2 audit and fixes: `similar --exclude-asset` repeatable and matching `src` or `local_path`; `blindcheck.py` gates the wave (asset ids and `snapshot:`/`source:` pointers in briefs and examples); the skeleton emits `> attrs:` (measured fields, `chrome=unanswered`, confidence, source) and `slots.json` carries style and attrs; `media_role` no longer demotes card-sized images on `logo`/`how ... works` alts (52 corpus rows became creatives, re-measured); `lp-bench` scores a run against its originals; skill and family docs updated (step 1.4 override rule, step 1.6 gallery annotations, step 6 bench, Signature checklist, chip text per page). `taxonomy.family_of` left total on purpose. | The audit found the live-2 workers were blind by luck, not design (all 36 exclusions were no-ops; S09 saw its own originals), the corpus `full-bleed` tags on the Recraft composites rested on unanswered chrome, and two creatives were kept from source by alt-word role guesses. Returning `None` from `family_of` when chrome is unanswered would have emptied 1225 `styles.yaml` entries and broken `similar --style` for most sections; the visible `> attrs:` line plus a manager rule is the smaller change. A `Read(./corpus/pages/**)` deny rule for workers is deferred: it would bind the main session too. |

## Branch

`main` is fast-forwarded to `milestone-1-corpus` after each milestone (last:
2026-09-04, Milestone 2). New work branches off `main`. No remote is
configured; the repo lives on this Mac only.

## Open questions

- Live check of the hooks and agents from `.claude/` needs a fresh session
  started in this folder (they load at start).
- The deny rule `Read(./**/.env.*)` also blocks writing `.env.example`; narrow it (e.g. `.env.local`) or create the file by hand.
- Worker model: `sonnet` (default now) or `opus`; decide on Milestone 3 reject rates.
- `page.png` (1440 px wide, full page) is stored but nothing reads it yet; the reviewer could compare against it.
- Snapshots must localise their CSS, not only their media: the 45 Next.js CSS
  chunks are content-hashed and picsart.com redeploys within days, after which
  the stale URLs return the HTML app shell (`nosniff`) and `dist/index.html`
  renders unstyled (runs/live-2, four days after the fetch; fixed by hand with
  the live page's chunks in `dist/css/`). `fetch` should download the chunks
  into `<slug>/css/` (absolutising root-relative `url()` references) and
  `lp-inject` ship them with `dist/`; fonts and the few
  `background-image:url(...)` assets can follow the same path.
- Ten inventory paths serve the client-side app shell (no server-rendered text). `fetch` now records them as `shell: true` in meta.json without a snapshot, so they never enter the DB; `discover` could drop them from `pages.yaml` by fetching each candidate once.
- Role guesses to watch in Milestone 2: every `feature-callout` video is `creative` unless the headline says "inside Picsart" or "built-in tools"; product-demo videos will slip through as creative.
- Style families cover hero and callout creatives; `gallery` (983 images) and thumbnails are untagged. `prompt-card` and the 16:9 grids have no compose template yet; heroes of composite families are briefed as `full-bleed`.
