# Plan: landing-page-gen

## Milestones

| # | Milestone | Exit criterion | Status |
|---|---|---|---|
| 0 | Environment: repo, CLIs, hooks, skills, agents, docs | `uv run pytest` passes; `lp-corpus --help`, `lp-inject --help` print; skill and agents load; hook smoke test passes | done 2026-09-04; deferred: live hook check needs a fresh session (hooks load at start), `.env.example` blocked by the workspace deny rule |
| 1 | Corpus: discover pages, fetch, sectionize, DB, `similar`, `skeleton` | pytest on a saved HTML fixture; `similar --type hero` returns hero sections from other pages with media; unquoted step costs measured | done 2026-09-04: 19 tests on `tests/fixtures/streamed.html`; `similar` returns one hero per page with PNG media; extend/edit/reframe quoted in `tool-map.md`; corpus = 199 pages: the 205 non-hub paths in `pages.yaml` minus 6 that serve the client-side app shell (`/ai-agent/`, `/ai-avatar/`, `/ai-enhance/`, `/ai-models/midjourney/`, `/ai-storyline/`, `/batch/`; recorded as `shell` in meta.json), built by `lp-corpus fetch --family ... --sectionize` and `lp-corpus media --all` |
| 2 | Dry run of the loop | `--dry-run` produces priced `workflow.yaml` per slot, reviewer critiques, `lp-inject` fills placeholders, ledger has zero paid rows | done 2026-09-04 on `runs/dry-1` (resize-image, 6 sections, 14 slots): every slot has a preflighted `workflow.yaml`, each section reviewed (5 of 6 needed one rework round), `dist/index.html` renders with 14 labelled placeholders, ledger has 0 paid rows; the manager procedure was run by the main session step by step, not via `/build-landing-page` |
| 3 | Live run, images only, ≤ 200 credits | every creative/thumbnail slot accepted or blocked with a reason; every brief names a style family and composite slots go through `lp-compose`; ai-template-generator S07 re-run reads as `template-mockup`; `dist/index.html` renders; spend matches ledger and `picsart_credits` delta | todo | 2026-09-08: composite check exercised outside the worker loop by `runs/trial-4` (`panel-overlay` at 4:3 and 5:4, tilted variant); a worker-driven composite slot is still to run |
| 4 | Video and tuning | hero video via still-to-motion; rework loop tuned; rules in `prd.md` | in progress 2026-09-19: live-5 delivered 5 clips (groundwork); the video-context layer (motion fields, strips, Motion lines, video recipe + rules, duration policy, poster, bench, job_status ledger) is built and tested; exit on the `runs/live-6` trial (3 video slots, faithful durations, ≤ 200 cr) |

Page inventory: `corpus/pages.yaml` (272 paths; families: tool 99, ai-models 87,
other 41, compare-models 18, hub 13, ai-tool 14). Regenerate with
`uv run lp-corpus discover --seed <url>`; the 2026-09-08 pass added 54 pages
from a depth-3 crawl that also expands `other` pages. The corpus holds every
family including `hub` and the home page (`home`); 10 inventory paths are
client-side app shells and have no snapshot. 262 pages, 3772 sections,
9255 media are indexed.
Snapshots live in `corpus/pages/<slug>/` (raw.html, page.html with `data-lp*`
stamps and local `media/` links, media/, page.png, render.json, meta.json,
sections.md); only sections.md and meta.json are committed. The 199
snapshots take 4 GB on disk (media, full-page screenshots, raw HTML); 9 of
4551 media nodes point at assets the CDN no longer serves (404) and keep
their absolute URL.

## Decisions log

| Date | Decision | Why |
|---|---|---|
| 2026-09-14 | **Data for accurate generation** (code layers). The corpus-library work improved browsability, not accuracy — the generation path read none of the new fields, and only 15/2740 assets carry semantic labels, so 92.6% of family resolution is the two measured photo families and retrieval ranked examples by section copy (no visual similarity), falling through to off-family images for 11 thin/empty families. Shipped the label-independent half of the four-layer plan: **(gate)** `styles.derive` marks a chrome-unanswered family `provisional`; **(L1)** `similar.find_similar` re-ranks candidates so a verified (chrome-answered) same-family, structurally-matching example outranks a provisional one and a text-only BM25 hit; **(L3)** `write_examples` attaches a `built:` construction field to each example; **(L4)** `feedback.py` + `lp-corpus feedback <run>` turn a run's `[resemblance]` flags on provisional originals into a re-label queue that `sheets.build` prioritises. 8 new/changed tests, 122 green. Remaining: the full labelling campaign (Layer 0, agent labor over ~303 sheets, composite candidates first) and the key-gated pool (Layer 2). | Accuracy is bounded by label completeness → retrieval quality → supply → feedback. The code layers are built first because they are label-independent and improve every run while the campaign proceeds; the gate stops a measured-only guess out-ranking a real example in the meantime. Plan: `~/.claude/plans/snappy-dazzling-creek.md`. |
| 2026-09-14 | **Corpus library**: `lp-corpus organise` rebuilds `library/`, a browsable generated hardlink tree — a folder per page (documents + full-page screenshot) and every image/video filed by who generated it (general vs a specific AI model) -> art style -> structure, each asset carrying a Markdown sidecar. Generated, not moved (zero bytes, dedupes, never dangles; relabel -> regenerate -> the asset moves folder), gitignored. Part A renamed the `finish` attribute to a richer `art_style` enum (photo, 3d-render, flat-vector, painterly-illustration, anime-cartoon, collage, typography, ui-screenshot, mixed), with an idempotent migration riding inside `label.ingest` (rewrites stray answers files, manifests and records; unmapped values dropped and reported). Part B added `taxonomy.structure_of` (a coarse 9-way structure computed on read from measured layout + labelled chrome, with a `structure_source`), Part C `taxonomy.model_of` (who made an asset, from "made with X" headlines and ai-models/compare page placements; link-grid thumbnails never evidence). Part D is `library.py` (assets, tree_path, sidecar, page dossiers, indexes, atomic tmp-swap rebuild). ~25 new tests; first real run: 2370 images, 382 videos, 136 screenshots, 262 pages, 31 by-model folders, most assets `_unlabelled` for art style until the /label-corpus pass fills them. | Areg wanted the corpus browsable by a person or an agent, each asset self-describing beside its file. The tree must be generated because paths are an API in three places and the 8-hex basename is load-bearing; a hardlink tree touches nothing. `art_style` and `structure` split what was one over-loaded `finish`/family axis; the 15 style families stay a sidecar field and still drive generation. Sidecars are a regenerated, gitignored projection (every field lives in committed sources), so nothing in `library/` is hand-edited. Supersedes the licensed-stock-pool plan (shipped 2026-09-10). |
| 2026-09-04 | Corpus comes from scraping live picsart.com pages with Playwright. | Pages are CMS-driven and client-rendered; no internal export is assumed. |
| 2026-09-04 | Output is re-injection into the scraped HTML snapshot, not a template render. | Highest fidelity; every new page starts from a snapshot of an existing one. |
| 2026-09-04 | Manager is the main session; workers and reviewers are subagents. | Subagents cannot spawn subagents; SendMessage keeps a worker's context across rework rounds. |
| 2026-09-04 | Workers author `workflow.yaml` before executing; reviewers critique steps, and rework re-runs from step N. | Reuses upstream URLs; makes prompts and model choices reviewable, not just the final pixel. |
| 2026-09-04 | Defaults: `gemini-3-pro-image` (Nano Banana Pro) for images, `seedance-2.5` for video, `seedance-2.0-mini` as video draft tier. | Areg's brief; quoted costs in the `picsart-workflows` skill tool map. |
| 2026-09-04 | Paid calls only via connector `b05f6314`, only after a preflight, only inside an active run; enforced by `hooks/credit_guard.py`. | Wrong-connector and unquoted spend are the two mistakes worth making impossible. |
| 2026-09-04 | Roles `ui-screenshot`, `icon`, `decorative` are kept from source. | AI cannot faithfully render product UI; icons are brand assets. |
| 2026-09-04 | Page discovery is a plain-HTTP link crawl from hub pages (`discover.py`); no sitemap exists (`/sitemap.xml` returns the SPA shell). Pages are server-rendered (1.4 MB HTML with links), so Playwright is a fallback for `fetch`, not the default. | Faster, no browser; verify per page that sections are in the static HTML. |
| 2026-09-04 | Hooks run on system `python3`, stdlib only. | Fast start, no dependency on the project venv. |
| 2026-09-04 | Dry run findings: workers plan well but copy the hero's composition into every section unless told that anchored means light and palette only; every first-round review asked for prompt changes, none for pattern changes; the rework loop via SendMessage worked (worker context intact, one round each); small thumbnails go to `gemini-3.1-flash-image` at a native ratio (3 credits) instead of the pro model cropped. | Lessons are in `prd.md`; the brief template should carry the anchored rule so reviewers stop repeating it. |
| 2026-09-04 | `check_result.py` reads the worker's own transcript (`agent_transcript_path`, falling back to `transcript_path`) and takes the section from the worker's first user message (its assignment), not from the first or last `sections/Sxx` path anywhere. | In the dry run a worker that listed the run folder was blocked for another worker's section (S06 asked to finish S11); in trial-1 the hook read the main-session transcript, which mentioned dry-1/S01 first, and blocked the S07 worker for S01. |
| 2026-09-04 | Skills, agents and hook wiring moved from the workspace `.claude/` into this repo's `.claude/`; hook commands are cwd-relative (`python3 hooks/...`) and `_ledger.current_run()` resolves `runs/current` from the hook file's own location. | Claude Code loaded this repo's `.claude/settings.json` (hooks fired live), but `$CLAUDE_PROJECT_DIR` resolves to the workspace folder, so anything path-dependent must not use it. The workspace copies had no version control. |
| 2026-09-04 | `fetch` gets the HTML by plain HTTP and reassembles the React streaming segments in Python (`<template id="P:x">` and Suspense `B:x` boundaries swapped for the hidden `<div id="S:x">` chunks, as the inline `$RC` scripts would). Playwright still runs once per page, for the full-page screenshot and the rendered box of every image and video. | The reassembled `<main>` was checked identical to the rendered DOM (31 images, 5 videos on `/ai-image-generator/`); slot sizes only exist after layout, so geometry comes from the browser. |
| 2026-09-04 | Before the full-page screenshot, inject `*{content-visibility:visible!important}`. | Section wrappers use `content-visibility:auto`; without the override everything below the fold is captured blank. `networkidle` never fires on these pages (analytics), so `load` + a scroll pass is used. |
| 2026-09-04 | A section is one non-empty direct child of `<main>`, plus the body-level `<aside>` (link chips) and `<footer>`. Type comes from the CMS component labels (`data-testid`, `data-pulse-section-group`: banner-block, promotional-component, how-it-works-section, use-cases, tutorials-section, faq-section, pricing-cards, ...) and falls back to structural heuristics (headings, media count, tabs, card links). | Labels are stable across pages and free; heuristics alone confused captioned galleries with link grids. |
| 2026-09-04 | Media roles are guessed: footer/header or ≤120 px → `icon`; card sections → `thumbnail`; alt words decoration/badge/logo → `decorative`; videos in how-it-works or "inside Picsart"/"built-in tools" callouts → `ui-screenshot`; else `creative`. Media with a 0×0 rendered box is dropped. | A wrong `creative` would spend credits on a UI shot; a wrong `ui-screenshot` only loses a slot. The skeleton is hand-edited before a run. |
| 2026-09-04 | `sectionize` stamps the snapshot: `data-lp-section="S03"`, `data-lp="S03-m1"`, `data-lp-t="S03-t2"`; the DB stores those selectors and `skeleton` emits `slots.json` with them. | `lp-inject` then needs no DOM-path selectors; the stamps survive any re-serialisation of `page.html`. |
| 2026-09-04 | Media `src` is stored as the CDN asset URL (Next.js `/_next/image?url=` proxy unwrapped). `similar` writes examples as PNG: AVIF/WebP converted with Pillow, videos as a still grabbed with Chromium at t=1 s. | Agents view files with `Read`, which cannot open AVIF or WebM; Playwright's bundled ffmpeg has no VP9 decoder. |
| 2026-09-04 | `skeleton.md` format: YAML frontmatter (`page, source, snapshot, brand, audience, defaults, budget, notes`), `## Sxx type`, `- tN tag: text -> href`, one fenced ```slot YAML block per media node (`id, kind, role, size, aspect, natural, duration_s, src, alt`), `> annotation:` per generated-role slot. `aspect` is quoted because bare `9:16` is a sexagesimal integer to YAML 1.1 parsers. | One file the manager parses and a human edits; ids map back to the stamps. |
| 2026-09-04 | Snapshots hold their media: `fetch` (and `lp-corpus media` for older snapshots) downloads every `img src`, `video src` and `poster` into `corpus/pages/<slug>/media/<stem>-<8 hex of the URL><ext>`, points the attribute at the copy, keeps the served attribute in `data-lp-src` / `data-lp-poster`, drops `srcset`, `sizes`, image preloads and `<picture>` sources, and removes `<base>` after absolutising the remaining relative links. `media.local_path`, `slots.json` `local`, the `skeleton.md` slot block `local:` and the `sections.md` media link (CDN URL as link title) all name the copy; `similar` copies from it. Stylesheets, fonts and CSS backgrounds stay remote. | Areg's call: a snapshot must not lose its images when the CDN drops an asset, and `lp-inject` can ship `media/` with `dist/`. Relative `media/` paths cannot coexist with `<base href="https://picsart.com/">`. |
| 2026-09-04 | `similar`: BM25 over section Markdown filtered by type, one section per page, source page excluded with `--exclude`, only sections with a `creative`/`thumbnail` slot, type-only fallback when the query matches nothing; at most 4 media files per example. | Briefs need media to look at, not the page's own section; 10 hero carousel images per example bloated a brief to 15 MB. |
| 2026-09-04 | Package uses a `src/` layout with one package and two console scripts. | Zero build-backend configuration with `uv_build`. |
| 2026-09-04 | Trial `runs/trial-1`: one random creative slot (ai-template-generator S07, 480x480) produced from the section text alone, source image, alt and paths withheld from the worker; 20 credits, one gate failure (a real brand mark inside a generated inset photo), balance delta matched the ledger. | Checks whether the brief carries enough for a worker without the original; the result is on-brief but generic (nature-photo cards) where the original is a bold branded template with tool icons, so the annotation, not the copy, decides the look. |
| 2026-09-04 | Repo declares an empty `[tool.uv.workspace]`. | `~/pyproject.toml` is a uv workspace; without this, uv adopts the project as a member and puts `.venv` and `uv.lock` in the home directory. |
| 2026-09-07 | Seven style families derived from contact sheets of the corpus creatives (`dark-composite`, `before-after`, `crop-frame`, `cutout-checkerboard`, `template-mockup`, `prompt-card`, `full-bleed`), documented in one skill file `picsart-workflows/style-families.md` with a fixed nine-key block per family; the manager copies one block into the brief. | Trial-1 showed the annotation alone yields a generic image; every real callout is a composite card with UI-like chrome, which no prompt rule described. One file: one `Read` for the manager, shared vocabulary once. Code says `style` (`> style:`, `media.style`, `similar --style`) because `family` already means the page family. |
| 2026-09-07 | Chrome (tiles, pills, brackets, checkerboard, badges, template card) is drawn locally by `lp-compose` (Pillow, templates as Python dicts in `compose/families.py`, geometry at 1600 px scaled to the slot's natural size, 2x supersampling, bundled OFL Manrope variable font, icons as 24-unit polylines). Workers generate only the photographic panels; a composite's `chosen:` is a run-relative path that `lp-inject` resolves against the run. | The no-text/no-UI rule stays intact for the model while the house style becomes reachable. Dicts over YAML: five templates, no loader. Corpus SVGs are store badges, not tool icons; Picsart's font is not recoverable from the snapshots. `picsart_media_upload` is a human drag-and-drop widget, so a local file cannot become a URL. |
| 2026-09-07 | Corpus creatives are tagged with a family by `lp-corpus styles`: Claude vision (`claude-opus-5`, structured output, ~884 hero/callout/use-case/how-it-works images, about $3), `alt` containing "before" tagged for free; persisted in `corpus/styles.yaml` keyed by CDN src and re-applied to `media.style` after every `sectionize`. `similar --style` prefers tagged sections and falls back to the untagged passes. Adds the `anthropic` dependency. | Role, size and type cannot separate the families (every candidate is a 480 px callout creative); a manual list of 30 would leave the preference inert. The yaml survives re-indexing; the SDK is the documented way to call the API. |
| 2026-09-07 | `template-mockup`'s headline box is chrome drawn by `lp-compose` (optional placeholder text or blank bars); the worker generates the template's photo only. | It is the one family whose real artefact carries readable text; drawing it keeps "images carry no text" true for the model. |
| 2026-09-07 | Trial `runs/trial-3`: one random creative slot (poster-maker S09, 480x480, `template-mockup`) produced blind, same prompt on `gemini-3-pro-image` (10 credits with one re-run) and on Gemini Omni (`gemini-omni-1.1-flash-preview`, 9 credits). Omni and Kling Omni are text-to-video only in the Picsart catalog, so the Omni panel is one frame of a 3 s static 9:16 clip grabbed with `FrameGrabber` (Playwright Chromium decodes the mp4). | Areg asked for the omni model beside the default; there is no omni image model. The worker preferred the Omni frame (single subject, even margin); the frame path costs an extra step (hand-computed crop anchor) and gives no `imageUrls` anchoring, so it stays a comparison, not a default. |
| 2026-09-07 | Blind-trial examples are picked by hand when `similar` returns sibling pages: motivation-/movie-/wanted-poster-maker serve poster-maker's CMS block and image. `check_result.py` matched `S07` from the contract example embedded in the brief and blocked the S09 worker on stop; `log_generation.py` logs async video calls with no URL (the clip URL comes from `picsart_job_status`). | Found in trial-3; fixes are open (exclude examples by asset id; take the section from the assignment, not the first path; hook `picsart_job_status` or write the URL from `workflow.yaml`). |
| 2026-09-07 | Picture text is generated. The skeleton gets a `> text:` line per generated slot (exact strings or `none`); the manager fills it from the section copy under the family's **Text** line; the brief carries a `## Text in image` table; the worker quotes the strings verbatim, ends prompts with ", no other text, no logos or watermarks", and gates spelling; the rubric gains a `text` key that is a hard reject below 5. Chrome text bound to compose geometry (pills, size labels, chips, buttons) stays with `lp-compose`; `template-mockup`'s headline moves to the model and the compose spec omits the headline item. | Areg's call after trial-3: the real poster-maker image carries "50% OFF" and "BUY NOW!" as artwork, which blank bars cannot stand in for. The manager, not the worker, decides the words so the image never invents copy and never duplicates the HTML next to it. |
| 2026-09-07 | Categorisation is derived from attributes: `lp-corpus attrs` describes every distinct generated-role asset once (ground, layout, panels, chrome, text, mockup, subject, finish, plus a family hint) into `corpus/attributes.yaml`; `taxonomy.family_of` is a deterministic rule table from attributes to `family[/ground]`; `styles.yaml` is derived from it (`styles --from-attrs`, manual entries kept); `lp-corpus taxonomy` writes the cross-tab and contact sheets; `media.attrs`, `media.style` and role fixes are re-applied after every `sectionize`. Slot classes (`<type>-<aspect class>[-video]`, `sectionize.aspect_class` / `size_class`) are the grouping key and the manager's lookup. | The seven families came from 54 hand-viewed images; the classifier saw only three doc lines per family and its confidence was never used. Attributes make the families auditable (counts per cluster), re-derivable without another model call, and let `similar` filter on ground or layout. Ground is the only variant axis this round. |
| 2026-09-07 | No vision API anywhere in the corpus pipeline. `lp-corpus attrs` measures what the pixels say (`corpus/measure.py`, numpy: ground from the border ring's median colour, checkerboard from block alternation on the top and left strips, gradient from a planar fit, panels and layout from background gutters, a full-height centre divider for `split` and, with a high row-profile correlation, `before_after`; `alt` containing "before" counts too) and writes partial records; `lp-corpus sheets` lays the assets that still lack a semantic field on numbered 12-cell PNG sheets grouped by (type, ground, layout) with a yaml manifest per sheet; a labeller (the `/label-corpus` skill fans out one subagent per sheet, or a person) writes `<sheet>.answers.yaml` keyed by cell number; `lp-corpus labels` validates every value against the enums, drops what is outside them and merges. `styles.py` keeps only load/save/apply/guide/derive; the `anthropic` dependency is gone, `numpy` is added. | Areg's call: the tagging pass wanted an API key and about $21, and the same eye is available for free in the session. The split is honest — pixels answer ground/layout/panels/before-after for 1496 assets in 87 s and place 55 % of them in a family with no judgement at all; only chrome, text, mockup, subject and finish need someone to look. Sheets are the unit because 12 assets of one measured group cost one `Read` and one short answer, and the manifest keeps the answers auditable per asset. Measured fields sit at the tail of `family_of`, so a labelled chrome or mockup still overrides the free guess. A pass writes only the records it measured (`merge_save`) and a re-measure keeps every answered field (`keep_answers`), because a long video pass otherwise clobbers a labelling merge that lands while it runs — observed once, five answers lost. Sheets are named `<type>_<ground>_<layout>-<6 hex of their assets>`, so a rebuilt sheet of different pictures can never inherit an answers file. |
| 2026-09-07 | Fourteen families: the seven kept and unchanged in id and order, plus `vs-two-up`, `mockup-card`, `cinematic-still`, `graphic-collage`, `outcome-tile`, `editor-canvas`, `model-card`. Untemplated families carry a **Template** line naming the fallback (`none; brief as X`), no chrome, or `kept-from-source` (editor-canvas and model-card thumbnails, which mock the Picsart editor). `dark-composite/light` and `template-mockup/black` are ground variants, not families. | Splitting on ground would double the compose templates for one fill colour; kept-from-source keeps the thumbnail tiles in the counts and in `similar` (their role stays `thumbnail`). Mockup cards are generated assets of their own family, not screenshots, per Areg. |
| 2026-09-08 | Coverage audit against the live site: every `img`/`video` served in a snapshot is compared with `media.src` (next/image proxies unwrapped). Three gaps found and closed: (1) two pages (`product-ad-maker`, `social-media-ad-maker`) had zero media because the site renders their content twice and the first copy measures 0×0 — `build_section` now takes the displayed copy's geometry for the same src instead of dropping the node (this also admits the five footer badges on every page, as icons); (2) 54 pages were never discovered because depth 2 stops at tool pages and `other` pages are not expanded; (3) 9 template-maker pages had swapped their thumbnails since 2026-09-04 (a content update, two live fetches agree) and were re-fetched. Hubs and the home page are now in the corpus. What is still not indexed, by design: media hidden at desktop width (93 distinct srcs, mostly the third-party model logos of the "works with" strip), CSS `background-image` assets (2 on `ad-maker`), inline SVG. | A corpus that misses a whole page's creatives skews `similar`; the drift check is cheap (plain HTTP, ~200 pages in 2 min) and worth repeating before a live run. |
| 2026-09-08 | First full-page live run `runs/live-1` on a random page (`hsl-color`, family `other`): 5 generated slots (hero 4:3 + four use-case cards briefed as a `full-bleed` Series), 21 kept, 0 blocked, 50 credits, balance delta equal to the ledger, `dist/index.html` renders. Blind: briefs carry no `src`/`local`/`alt`, `similar` excludes the page and its asset ids, the manager opens the originals only after the last verdict. The manager relabelled the "Featured In" gallery `decorative`. | Milestone 3's loop ran end to end with the skill's procedure (manager in the main session, workers and reviewers as subagents, SendMessage rework). Findings: `check_result.py` blocks every stop with a stale `S09` (still open), the `count: 1` rule never reached the worker, an `imageUrls` anchor leaked the hero's subject; rules in `prd.md`. Not yet met from the Milestone 3 criterion: no composite family exercised (the random page had only full-bleed slots) and no template-mockup re-run. |
| 2026-09-08 | Fifteenth family `panel-overlay` (a photo filling the slot with the tool's dark adjustment panel over it; heroes carry a round tool badge and a label pill instead; `/white` = the tilted stacked-card variant), with an `lp-compose` template that accepts 4:3 and 5:4, new chrome kinds `adjust-panel` and `tool-pill`, a `ground: tilted` spec option, the `adjust-panel` chrome attribute, and a rule in `family_of`. Derived from `research/live-1-style-gap/`: all five hsl-color originals are stock photos (the hero is Pexels 2180474 by R. Fera) with this chrome, which the measurer reads as `photo-full-bleed / single` and the unanswered sheets left as `full-bleed`. Checked in `runs/trial-4` (15 credits, three `gemini-3-pro-image` renders of the stock look composed with the template). Label sheets rebuilt without `--skip-resolved` (299 sheets) so resolved-but-unlabelled assets get a sheet; the three sheets holding hsl-color are answered for its cells. | The live-1 gap was structural: no family described "photo + floating editor panel", so the workers produced on-copy editorial photos with no chrome. The same chrome recurs on hair-color-changer and ai-filters, and Fotor's and VSCO's HSL pages use the same device, so it is a category convention, not a one-page quirk. |
| 2026-09-08 | Every style family gets external references of its photography: `/collect-references` spawns one `reference-collector` agent per family (Pexels and Unsplash only, every URL verified on its page, no downloads, no paid calls) writing `corpus/references/<family>.yaml` (genre, search terms, 5 to 10 examples with creator and licence, creators, what is Picsart-specific, prompt guidance, open items); `apply.py` validates and writes a **References:** line into each family block; a test pins the files to `db.STYLES` and the schema. First pass done for all 15 families (9 to 10 examples each, 3 to 6 creators). Wave 2 (`--pages`, one agent per page: which family each creative really is, chrome present or absent, photo sources) is defined but not run. | Areg's ask after trial-5: the corpus examples show layout, not where the photography comes from; hsl-color's images were Pexels stock, and a worker prompting the stock genre lands the look. Pexels and Unsplash because their licences allow the comparison and their pages name the creator. Finding across the pass: Unsplash's matching shots are mostly Unsplash+ premium, so Pexels carries most references. |
| 2026-09-08 | Run loop trimmed for tokens and wall time: workers and reviewers no longer open `style-families.md` (the brief carries the family block), briefs get 2 examples at one 480 px image each (`similar -k 2`, `--media-per-example`, `EXAMPLE_MAX_PX`) plus a `## References` section from `corpus/references/<family>.yaml`; the direct pattern is `count: 1` at 1K (2K only above 1000 px natural width); anchoring is by words from a `shared-context.md` written before the wave, so all sections spawn at once; a `precheck.py` paperwork check replaces record-only review rounds and one reviewer spawn covers the whole wave; `check_result.py` takes the section from the assignment sentence (else the most-named existing folder); `report.md` gains an "Agents" table (tokens, minutes per spawn). | Areg asked for every possible optimisation. live-1's ledger shows the paid calls took about 4 of 20 minutes; the rest was sequential waves and per-section review rounds, and each agent spawn re-read about 16k tokens of skill text plus a dozen example images. The stale-section stop bug burned a retry on every worker. To be measured on the next live run against live-1 (50 credits, 8 generates, 2 review rounds per section). |
| 2026-09-08 | Second full-page live run `runs/live-2` on a random page (`ai-models--recraft-v4-styles-pro-vector`): 16 generated slots (hero, 8 gallery tiles, 3 callouts, 4 tutorial thumbnails), 20 kept, 0 blocked, 112 credits, balance delta equal to the ledger, `dist/index.html` renders; all six sections spawned in one wave, precheck caught two record problems without a review round, one reviewer spawn per round (the first hit its 30-turn limit after four sections and was resumed). Every vector slot's annotation overrode the family's photograph wording with a flat-vector finish and a shared palette written from the skeleton. | Milestone 3's loop with the trimmed run procedure: 34 minutes for 16 slots against live-1's 20 for 5, worker-bound (the S03 Series gated 18 flash calls serially). Against the originals: the four callout/hero slots are black `dark-composite` cards with a mark tile and an SVG chip that the measurer had tagged `full-bleed` (unanswered sheets, same on the sibling pages), and the gallery originals fill the tile on saturated grounds where the brief asked for margin on off-white; rules in `prd.md`. Still not met from Milestone 3: no composite family exercised by a worker, no template-mockup re-run. Reviewer `maxTurns: 30` covers about four sections per spawn. |
| 2026-09-09 | Post-live-2 audit and fixes: `similar --exclude-asset` repeatable and matching `src` or `local_path`; `blindcheck.py` gates the wave (asset ids and `snapshot:`/`source:` pointers in briefs and examples); the skeleton emits `> attrs:` (measured fields, `chrome=unanswered`, confidence, source) and `slots.json` carries style and attrs; `media_role` no longer demotes card-sized images on `logo`/`how ... works` alts (52 corpus rows became creatives, re-measured); `lp-bench` scores a run against its originals; skill and family docs updated (step 1.4 override rule, step 1.6 gallery annotations, step 6 bench, Signature checklist, chip text per page). `taxonomy.family_of` left total on purpose. | The audit found the live-2 workers were blind by luck, not design (all 36 exclusions were no-ops; S09 saw its own originals), the corpus `full-bleed` tags on the Recraft composites rested on unanswered chrome, and two creatives were kept from source by alt-word role guesses. Returning `None` from `family_of` when chrome is unanswered would have emptied 1225 `styles.yaml` entries and broken `similar --style` for most sections; the visible `> attrs:` line plus a manager rule is the smaller change. A `Read(./corpus/pages/**)` deny rule for workers is deferred: it would bind the main session too. |
| 2026-09-09 | Third full-page live run `runs/live-3`, the benchmark re-run of live-2's page with the audit fixes: 18 generated slots (S06 and S08 now creatives), 18 kept, 0 blocked, 126 credits, balance delta equal to the ledger, `blindcheck.py` clean before the wave, `dist/index.html` renders (CSS localised), `lp-bench` 25 flags on 18 slots against live-2's 38 on 16, family match 0.44 -> 0.61, coverage gap -0.25 -> -0.14, ground lightness gap +0.37 -> -0.07. Two review rounds plus one manager exception (a targeted edit on S09-m3). Template fix landed mid-run: dark-composite tiles at #1c1c1e. | The fixes did what they were for: the composites derive the originals' family, the gallery sits on saturated grounds and fills 85-100 % of the height. What is left is measured: the gallery's coverage gap (-0.29, target 0.15) is width, not height (originals crop figures at the frame), and the manager's shared context assigned no ground per composite. Record habits (attempts folded into one step) cost three zero-credit fix rounds and are now in the worker definition. Reviewer `maxTurns: 30` is workable with a one-Read-per-asset budget; raising it is the alternative. |
| 2026-09-10 | Fourth full-page live run `runs/live-4` on a random page (`birthday-card-maker`, family tool): 20 generated slots (hero, how-it-works, 15 gallery tiles, 3 callouts), 20 kept from source (icons, badges, and the 6 "Featured In" press logos relabelled decorative as on live-1), 0 blocked, 143 credits (113 wave + 30 rework), balance delta equal to the ledger, `blindcheck.py` clean before the wave, `dist/index.html` renders (hero + gallery + callouts injected; lower sections show the known stale-CSS-chunk drift). First live run to exercise a **worker-driven composite family**: the three callouts are `template-mockup` composed through `lp-compose` (Milestone 3's open composite criterion), and the photo-panel gate caught a printed-card/book-cover/bordered look on all three first attempts before compose. `lp-bench` 26 flags on 20 slots, family match 0.65 (dragged by `chrome=unanswered` originals that flag on any resolution); gallery tiles fill more of the frame than the card-thumbnail originals (the fill rule). SendMessage is disabled this session, so the one rework round (S05 gallery consistency: 8 of 15 tiles photographed the card as an object) ran as a fresh worker scoped to the 8 tiles from result.md/workflow.yaml. | The loop runs end to end on a template/card page with no code change: the manager decides family (gallery full-bleed + per-tile text, callouts template-mockup), text and the press-logo drop; workers stay blind. Two structural notes for template pages: the gallery/callout originals are designed cards, not photos, so the flat-illustration clause must be pinned or the model renders a card-object (prd.md); and template-mockup draws one photo panel while the callout originals show 2-3, so the `[fit]` gap parallels the dark-composite device work (a multi-panel template-mockup variant is the fix). Operational: the connector's Drive quota was full, so every S05 call needed `saveToDrive: false`; and rework without SendMessage works by re-scoping a fresh worker off the on-disk record. |
| 2026-09-09 | Device layer, Phase 1: every generated slot gets a `> device: <id>: <claim>` line (none, reference-thumbs, icon-set, two-up, model-picker, applied-mockup) that the manager decides from the copy's cue words and the example images (skill step 1.7); `lp-compose` gains `variant:` on `dark-composite` (reference-thumbs, model-picker, two-up, geometry from the Recraft originals) and a `list-panel` chrome kind (blank rows, the page's own model name on the active row, never a competitor's); `fit` is scored against the claim and gated at 4; `precheck.py` checks the compose variant against the brief; `lp-bench` counts picture panels (`pictures`) and flags composites that lost the original's device. Trial `runs/trial-6` (five slots of the live-3 page, 52 credits, one wave, one review, no rework): every asset carries its device, `fit` 5 on all five, zero `[fit]` flags against live-3's four, balance delta equal to the ledger. Areg's scope: Phase 1 only; the device attribute in the corpus (card-colour measurer, `device_of`) and the reference-data tooling (research folder, reverse-image procedure, CLIP ranker, quarantined competitor crawl, `--pages` wave) stay planned in `~/.claude/plans/so-far-the-image-shimmering-beacon.md`; the creator and source list is `research/context-gap/creators.md`. | live-3's composites matched the family (`lp-bench` family match 0.61) and missed the point: opened at full size, every original is a small narrative (S01 reference thumbnails beside the output, S04 a 3x3 icon set, S06 two outputs, S07 a model picker, S08 a logo on a shop sign) and our template drew one panel plus two generic tiles. The chain thinned the meaning at four hops: the annotation was subject and composition only, the worker was told to prompt from the References genre not the copy, `fit` had no gate, and `lp-bench` measured pixels only. The corpus cannot yet be the oracle (the originals' panels touch the frame and measure as one picture; 4 of 306 sheets answered), so the manager decides the device and the pixel proxy flags the loss; a measured `device` attribute is Phase 2. |
| 2026-09-10 | Workflows restructured as **Picsart Flow boards**. Picsart Flow (`picsart.com/create/workflows`; gallery `picsart.com/workflows`) is Picsart's own node-canvas workflow tool: input (START/REF/prompt) → Image/Video/Text/Audio/Motion nodes, one model each → END, saved as typed Workflows and shared as gallery templates. It has no API (the editor is login-walled; the API platform's `/workflows/{name}/execute` endpoints are per-model runs, not graphs; the MCP connectors expose the engines behind the nodes). So workers author boards in Flow's model and run them on Flow's engines: `workflow.yaml` gains `board: blank|template`, `template:`, `start:`, and per-step `node:` (text, ref, image, edit, cutout, background, enhance, video, motion, compose) and `in:`; `lp-flow check` validates wiring, engine per kind, the pro-model rule and template provenance; `lp-flow sheet` renders `flow.md`, the node sheet a person rebuilds on the canvas; `lp-flow templates` searches `corpus/flow-templates.yaml` (seeded with the two gallery templates read first-hand; the manager refreshes it from the gallery in step 0). Blank board is the default; a template is copied only when it fits the family and device, covers every panel and moves no invariant. Recipes in `image-workflows.md` are re-expressed as node shapes and gain `layered` (Routes 1 and 2 with the harmonisation gate, from the pilot). Reviewer rubric gains `board`; `precheck.py` runs the board check; old records (live-1 to live-4) read as boards through inference. Corpus widening: `lp-corpus widen <family> [--exact]` reverse-image-searches every tagged asset of a family (SerpApi Google Lens or Cloud Vision web detection, stdlib only) into `corpus/widened/<family>.yaml`; `similar --widen N` adds neighbours to a brief as `w<n>-widened.md`, look-only. | Areg: agents must work within Picsart's own workflow tool, blank board first, templates permitted when most appropriate, and the manager needs a way to pull a larger corpus (Lens-search the corpus images). The honest shape of "within Flow" given no API is Flow's model on Flow's engines plus a canvas sheet; anything else would be pretending to drive a GUI we cannot reach. The Lens equivalents are the two APIs; Bing Visual Search is retired. Full analysis in `research/flow-restructure/analysis.md`. |
| 2026-09-10 | Model discipline tightened and workflows deepened after the live-4 review. `gemini-3-pro-image` is now the strict model for **every** image — finished slot, gallery tile and composite thumbnail alike; a non-pro model is used only when the section copy tied to the image explicitly names the model that generated it (a model-showcase/compare page), so the demonstration is honest, and the naming copy is quoted in the step. The old "flash acceptable for a one-or-two-word string" clause and the model-less `series` pattern were the two seams that let 15 finished gallery tiles run on flash. `image-workflows.md` gains an **Anchor to the corpus, then vary** doctrine (invariants — finish, palette, light, Panels/Never, crop, text — are the fixed spine; everything else is deliberately varied per slot so no two are interchangeable) and a **Longer workflows** section (generate → critique → i2i refine → variation pass, each preflighted and gated). Per-slot cap 20 → 40 and run cap 300 → 600 in `skeleton.py` BUDGET (`tests/test_corpus.py` updated); `tool-map.md` relabels flash "composite thumbnails only". | Areg: nothing on disk actually said "pro-model unless the text calls for another", so the worker took the cheaper flash under a draft-tier clause meant for thumbnails — a real doc defect, not a worker error. The corpus was being copied as a stencil, giving a gallery that varied only by headline string; the fix makes it the floor and mandates controlled variation. Bigger budget buys depth (refine + variation on the pro model) rather than more one-shot slots; video cap left at 60 (out of scope). |
| 2026-09-10 | First run on the Flow-board format: `runs/showcase-1`, 15 standalone images from a hand-authored skeleton (no source page), 12 sections across 10 families + the `two-up` device + one picture-text slot. 13 of 15 delivered, 10 accept / 0 rework / 2 blocked, 115 credits (23 pro-model generates, balance delta = ledger exactly), every board blank after the workers rejected the loose catalogue match on the three fits tests, 7 gate catches all fixed in one node each (logo, signage, under-fill, monochrome, inset card, framed border, series product drift). S05 and S08 blocked on the Picsart Drive quota: enhance/remove_bg/change_bg have no `saveToDrive:false`, so a full Drive stops every derivative panel; both records carry exact resume points. Review needed 3 spawns: the 12-section reviewer hit its 30-turn limit having written nothing; 6+6 with write-as-you-go worked, plus one single-section spawn. | The board format survives contact with a real wave unchanged; the failure modes were operational (Drive quota, reviewer turn budget), not structural. Rules to prd.md: reviewer spawns cap at ~6 sections; check Drive headroom before derivative families; unquotable aspects get nearest-enum + recorded delivery crop. |

| 2026-09-11 | **Flat-rate edit tools unblocked in a run** (`hooks/_ledger.py` `FIXED_PRICE`, used by `credit_guard.py` and `log_generation.py`). `picsart_remove_bg`/`change_bg`/`enhance` return `credits: null` from preflight (generate-only), so the guard's "no preflight quote → deny" rule made them unusable inside a run — the pilot-layered blocker that forced Route 2 into an i2i workaround. The guard now prices them from a fixed table (remove_bg 0, change_bg 2, enhance 2, topaz 3, from tool-map.md), allows them without a preflight, and counts the fixed cost toward the run cap; the ledger records the fixed price. 3 hook tests added. | Sophisticated `layered` Route 2 (plate → subject → true cutout → harmonise → enhance) needs the real edit nodes, not a workaround; they are flat-rate and known-priced, so a fixed table is the honest guard, not a preflight that can never return a number. Generate models still require their preflight — unchanged. |
| 2026-09-11 | **Overlap review and brief-building with the wave** (manager loop, no generation change). Two serial manager-thread stretches bookended the parallel worker wave and held most of the wall time. (1) Review was one whole-wave `section-reviewer` spawned after the last worker; it is now spawned per section (≤3 that clear precheck together) the moment each worker passes precheck, so review runs while later sections still generate and no spawn nears its turn limit. (2) `blindcheck.py` gained a per-section form (`blindcheck.py <run> <Sxx>`, page-wide ids still read from `slots.json`); the manager now blind-checks and spawns each worker as its brief is written instead of waiting for the whole set, with a whole-run check kept as a backstop. `build-landing-page/SKILL.md` §2/§4/§5 rewritten; the reviewer agent and hooks unchanged (still one run, one ledger). | Areg wanted more parallelism, but a timing pass on showcase-1 showed the paid Picsart calls were never the bottleneck (all 53 across 12 workers inside a 5.8-min window; images ~20s, already overlapping) — 88% of tool activity was the agent loop, and the serial tail was review + pre-wave brief assembly. So the win is overlapping those two with the wave, not parallelizing generation. Deferred as low-value or larger scope: parallel in-board branches (agent still inspects each result serially) and multiple sharded managers (needs `LP_RUNS_CURRENT` run-isolation; only helps across many/large pages). Supersedes the reviewer-size caps in prd.md. |
| 2026-09-10 | **Stock pool**: a fourth reference tier under `corpus/pool/<family>.yaml` — Pexels/Unsplash photos found by the family's own `search_terms`, deduplicated by 64-bit dHash (`phash.py`, the repo's first content-level identity; mirror hash too) against the corpus (`_hashes.yaml`, hamming ≤ 10, deliberately loose so a pool photo that IS a corpus source is always dropped) and the pool itself (≤ 6), ranked 0.7·histogram-intersection-with-top-5-nearest-tagged-assets + 0.3·measured-field frequency (before-after ranks on ground only: its corpus assets measure `split`), kept/dropped from contact sheets answered like the label sheets (`lp-corpus pool search/sheets/labels`). Served by `similar --pool N --seed <run> [--pool-aspect]` as `p<n>-pool.md` + 480 px PNG via a sibling of `widen.write_examples`; rotation is a seeded shuffle on the run id, nothing written at read time. Kept Unsplash photos get their download ping at keep time (the demo rate cap makes per-view pings impossible). Pilot scope: full-bleed, cinematic-still, before-after. | Areg: expand the corpus with unique external images per family so workers vary instead of copying the same two excerpts (the data side of corpus-as-floor). Stock APIs only — licence known, creator credited, zero credits; SerpApi widening and competitor crawls deferred. pHash + simple features, no CLIP: no new dependency, and ranking only orders the human sheet pass. Brief budget unchanged (2 corpus + ≤ 2 pool images): variety comes from rotation, not volume. |
| 2026-09-22 | **Composition labelling campaign** (layered overhaul, branch `layered-templates`, plan `~/.claude/plans/we-need-to-overhaul-harmonic-bird.md`). `lp-corpus sheets --composition` lays every chrome-answered asset whose `chrome_items` were not human-labelled on sheets of one chrome combo each, the bag pre-filled, so a labeller only places the known kinds. 1,550 assets / 449 sheets packed into 150 batches; each batch placed by one agent and re-read + corrected by an adversarial verifier (`high` effort, place inherits `max`), sharded across 3 workflows. Ingest: 1,467 cells (82 left out as too small to judge, 1 over the 40-char text limit), chrome-items-pending 1,091 → 69, doctor 0 errors, resolve share 0.8598 → 0.8629; answers kept in `corpus/labels/composition/`. | The labels exist for the composite benchmark and retrieval: `lp-bench`'s `[composition]` check needs labelled originals and had none; `composition_of`/`device_of` now read human placements. The resolve-share gain is small by design (the rule table mostly reads the bag, already answered). Verify at `high`, not `max`: Areg's call. Sharding: the workflow runner caps 6 agents per workflow on this 8-CPU Mac. A first run was discarded because agents wrote zoom crops with generic names into the shared folder. |
| 2026-09-22 | **Compose preset wave 1** (proposals from a 9-agent corpus-evidence workflow; authored serially in one worktree, full suite per commit; independent 4-dimension review). MAGENTA → `#e01ee0`; before-after inverted (the wide 21:10 card is the default, the 1:1 stack is `variant: stacked-square`, selected by the slot's size via `compose.cli.preset_for_size`); crop-frame `crop-grid`, template-mockup `palette-card` (swatch stripe) and `selection-frame` (also on cutout-checkerboard), each opt-in by `> device:`. Review fixes: keep-clear semantics, undrawable style grounds, per-slot sizes in brief and precheck, the new devices named in the skeleton and SKILL step 7, family docs matched to the code. | Evidence over the old single-exemplar defaults: 11 of 15 genuine before-after composites are the wide card; every new variant cites ≥ 10 corpus assets. Variants rather than new default chrome, so no existing golden moved (08's swap is pixel-identical). Not authored: 06 multi-aspect and 09 S02/S08 touch before-after together and go in as one reconciled pass sharing one divider; 05 editor-exploded builds on 03 + 04; 01 compare-labels and 07 per-panel-ground have 2–3 clean exemplars and are re-scoped. Section-specific chrome values (crop ratio label, swatch colours) stay template placeholders until the manager `build()` richer half (roadmap 4). |
| 2026-09-23 | **Slot size and page strings are settled before any credit** (3806a84, 697b554). `plan.validate` applies lp-compose's own aspect check (`cli.fits`) to the plan's preset and size, so `brief.py` stops on a slot no layout fits and names the one that does. The page strings a composed layout draws come from a new skeleton line, `> chrome: "a" \| "b" \| none`, which `brief.py` applies to every plan through `families.LABELS` (each preset's slots, in `lp-compose --describe` order); it refuses a TODO on a layout that draws page strings and a string also on `> text:`, `validate` refuses extra strings, and precheck `label_problems` holds the spec to the plan. | Both close a gap where credits were spent before anything failed: an unfit size failed only at render, after the panels; a placeholder label (a blank active row, "HSL" on any adjustment tool) shipped unless the manager hand-edited the generated plan, which `brief.py` rewrites on every run. The design's `> chrome: <preset\|auto>:` prefix was dropped, since `> device:` already picks the preset. A sweep of past skeletons found one real unfit slot: recraft-v4-styles-pro-vector S08-m1, a 879x418 prompt-card. Still plan edits: panel state (hue chips on a non-HSL tool, one-slider cards) and `select.rect`. |
| 2026-09-23 | **Layered-overhaul scope cuts** (Areg). Not merged: the 16:9 before-after `split` and vs-two-up `cards` pass (branch `worktree-agent-af1bc2380038df4a9`, kept for reference). Dropped: preset 01 compare-labels. Deferred: 07 per-panel-ground, until a compare-models page runs live. `style-families` pushed to its six local commits, so PRs #6 and #7 show only their own work. The preset proposals 00–09 copied to `research/preset-research/`. | Evidence, not effort: 0 of 123 before-after and 0 of 54 vs-two-up corpus assets sit in a 16:9 slot, so both layouts would template a shape no real page uses, and the size check refuses such a slot instead. 01 has 2 clean exemplars plus a cousin and 07 has 2, under the 10-asset bar. The six commits were already public inside both PR branches, so the push published nothing new. |
| 2026-09-23 | **Page grammar: when Picsart pages use what** (plan `~/.claude/plans/create-a-plan-that-keen-thunder.md`). New `corpus/grammar.py` + `lp-corpus grammar [--write-doc]`: one context row per generated-role slot (11,055 on 466 pages; immediate context = section type, size, aspect, headline words, copy cues; wider = page family with sub-pages inheriting the parent's, position band, neighbouring types, other videos on the page) against the original's decisions → `corpus/grammar/grammar.yaml` (section order per page family, backoff priors for video share / family / motion / length, context → decision rules by support and lift, motifs, copy themes) + `report.md` with example slots; `page-grammar.md` rendered from the yaml. `skeleton` writes a `> prior:` line per slot (plus a section note where media is the norm), with `# unusual:` when the original breaks a well-supported pattern (4 of 11,055 corpus slots); `brief.py` takes the prior's median length for a video with no known length (was a flat 5 s), falls back to the context's motion when a family has no Motion line, and adds `## Where this section sits`; `doctor` warns on a stale grammar and errors on a hand-edited doc. | Areg: understand page structure, when images vs videos, video lengths, recurring themes, when to apply what — **advise and fill gaps**, never override the original (the skeleton copies each slot's kind and length from the source page; a manager switching them would stop being faithful). Counting + lift, stdlib only, deterministic and 2 s to rebuild, so it is a derived layer like `styles.yaml`, not a model. The strongest finding is context, not type: a callout after a video section is a video 91 % of the time (vs 26 % of callouts), a headline saying "video" 88 %; galleries, link-grids and how-it-works never are; clips run a median 8 s (IQR 6-10), mostly ui-demo. Model-name cues use a word list, not `compose/models.yaml` (21 image models, uncommitted work on this branch). **Critical pass, same day:** a held-out check (every fifth page cluster) showed the length prior is the one real gain (error 3.6 s vs 5.2 s for flat 5 s), the family prior is no better than the commonest family (top-1 34 % vs 33 %), and blending the context rules into the video share doubled its Brier (0.047 vs 0.022). So the prior is now the plain backoff table, the rules describe the corpus only, a rule needs 3 page clusters (4 of the top 12 had been one CMS template), copy themes count distinct headlines, and `evaluate` writes these numbers into the doc on every build. |

## Branch

`main` is fast-forwarded to `milestone-1-corpus` after each milestone (last:
2026-09-04, Milestone 2). New work branches off `main`. No remote is
configured; the repo lives on this Mac only.

## Open questions

- Live check of the hooks and agents from `.claude/` needs a fresh session
  started in this folder (they load at start).
- The deny rule `Read(./**/.env.*)` also blocks writing `.env.example`; narrow it (e.g. `.env.local`) or create the file by hand. `PEXELS_API_KEY` and `UNSPLASH_ACCESS_KEY` (the pool's two keys) still need lines there.
- Worker model: `sonnet` (default now) or `opus`; decide on Milestone 3 reject rates.
- `page.png` (1440 px wide, full page) is stored but nothing reads it yet; the reviewer could compare against it.
- Snapshots must localise their CSS, not only their media: the 45 Next.js CSS
  chunks are content-hashed and picsart.com redeploys within days, after which
  the stale URLs return the HTML app shell (`nosniff`) and `dist/index.html`
  renders unstyled (runs/live-2, four days after the fetch; fixed by hand with
  the live page's chunks in `dist/css/`). `fetch` should download the chunks
  into `<slug>/css/` (absolutising root-relative `url()` references) and
  `lp-inject` ship them with `dist/`; fonts and the few
  `background-image:url(...)` assets can follow the same path.
- Ten inventory paths serve the client-side app shell (no server-rendered text). `fetch` now records them as `shell: true` in meta.json without a snapshot, so they never enter the DB; `discover` could drop them from `pages.yaml` by fetching each candidate once.
- Role guesses to watch in Milestone 2: every `feature-callout` video is `creative` unless the headline says "inside Picsart" or "built-in tools"; product-demo videos will slip through as creative.
- Style families cover hero and callout creatives; `gallery` (983 images) and thumbnails are untagged. `prompt-card` and the 16:9 grids have no compose template yet; heroes of composite families are briefed as `full-bleed`.
- Flow boards are unexercised in a live run: the first run on the new format should measure how often a worker copies a template versus starting blank, whether `lp-flow check` catches anything `precheck.py` did not, and whether `flow.md` shortens the review. `corpus/flow-templates.yaml` holds two templates; a full gallery pass (step 0) needs the browser pane visible.
- `lp-corpus widen` has no key on this machine yet (`SERPAPI_KEY` / `GOOGLE_VISION_API_KEY`); the module is tested against recorded payload shapes, not a live call. First live use: one family, `--limit 5`, check the yaml by eye. `.env.example` cannot be written under the workspace deny rule; the key names live in `CLAUDE.md`.
- Planned recipes (2026-09-14): Areg wanted multi-step workflows planned in advance and enforced, not grown reactively from gate failures. Added `RECIPES` + `recipe_problems()` to `flow/board.py` (fires from the board's `family:`), a `recipes.md` companion, rewrote the reactive language in `image-workflows.md`/`flow-boards.md`/SKILL/section-worker/brief-template, and 5 flow tests. The recipe is the floor (a slot may plan more, never fewer); over-cap boards drop the optional extras (2nd refine, variation) first, never a recipe node. Next: run a page with the new enforcement and measure the credit/quality delta vs page-logo-maker's shallow boards.

| 2026-09-15 | Fifth full-page live run `runs/live-5` on a random page (`persona`, family other), first run to generate **images + video together** end to end, budget lifted to "unlimited" (cap 900 then 1200). 35 generated slots (30 images + 5 videos) across 12 sections, 11 icons kept; all 12 sections accepted, 0 blocked; `dist/index.html` renders (35 filled, 0 placeholders). True spend 902 cr (balance 1341->439); recorded section spend 712, the ~190 gap being duplicate/orphaned video renders from the manager over-spawning resumes plus the topaz-via-generate ledger under-count. `lp-bench` family match **0.90** (27/30; 3 deliberate composite->full-bleed overrides), the best of the live runs (live-2/3 0.61). Every board blank (no gallery template fit the photo families). | Exercises the full loop under three real stresses: a Drive-quota 403 that hard-blocks enhance and the video chain (worked around with topaz-upscale-image via picsart_generate, no Drive deletion), SendMessage disabled (manager did free record fixes and re-spawned rework), and the 80-turn worker cap that stops big/video sections mid-way. The over-spawn waste and the workaround/ledger gaps are written up in prd.md as the next fixes. Milestone 3's remaining criteria (composite family exercised: S10 dark-composite; blind: blindcheck clean) met; this is also the first live video delivery (Milestone 4 groundwork). |

| 2026-09-15 | **Compose-template audit and correction.** Six parallel sub-agents each audited one lp-compose template against its tagged corpus population (≥10 assets sampled and viewed per family), writing evidence + a corrected 1600px spec to `research/template-audit/<family>.md` (+ `STANDARD.md` index and standing rules). Verdicts: dark-composite and template-mockup FALSE, before-after/crop-frame/cutout-checkerboard half-faithful (single-exemplar), panel-overlay faithful geometry with renderer bugs. Instituted the corrected DEFAULT for each family in `compose/families.py` (transparent grounds where the alpha proves it, black for crop-frame; dark-composite left column with a magenta accent and no invented 4K chip; template-mockup transparent + 3 mixed tiles + card-as-design; before-after wide card as a `variant`; crop-frame dimmed source + bare icon; panel-overlay squircle chips/white ring/teal-circle/tool-pill proportions) plus three new draw primitives (colour-swatch tile, bare `icon` kind, panel `dim`). Each re-rendered with a real portrait and eyeballed against the cited corpus asset; `style-families.md` Ground/Palette lines corrected; 134 tests green (compose/flow tests updated). | The dark-composite "3 boxes not 4" question exposed that the templates were built from one asset each and never re-checked against the population, so they drift as the corpus re-tags. The fix is both the corrected templates and the standing rules (STANDARD.md) that keep them honest: template the modal, measure ground from alpha, cite living assets. Deferred (need new worker panel inputs or new primitives, each its own verification): partial-fill checker, template-mockup selection-frame/swatch-stripe/editor, crop-grid and label-card variants, dark-composite compare-labels/bottom-row, cutout light-checker tone. Data follow-up: clean the noisy tag populations and add an Examples-line consistency test. |

| 2026-09-16 | **Stock API traffic is metered by the code itself.** New `corpus/apiclient.py` (sliding windows per platform at 90% of each published free tier, read from a persistent request log so a resumed run cannot re-trip the hour cap; exponential backoff with jitter honouring `Retry-After` and `X-Ratelimit-*`; typed per-platform errors) and `corpus/ledger.py` (one gitignored sqlite: a response cache with a 7-day TTL and a 24-hour floor for Pixabay's terms, plus the request log that *is* the budget). `pool.search` rewritten around them: breadth-first pagination (`--pages`, every term sees page 1 before any sees page 2), per-platform isolation (a 429 or an auth failure stops that platform, not the run), metadata prefilters that reject on orientation/width/licence before a byte is spent, `--dry-run`, `--max-requests`, `--refresh`, and a per-run report appended to `corpus/pool/_runs.jsonl`. Pixabay added as a third platform; a missing key now skips its platform instead of blocking the run. | The hooks in `hooks/` match MCP tool names (`picsart_*`) only, so a `uv run lp-corpus` HTTP call is invisible to them: unseen, uncapped, unledgered. Nothing above the client could enforce a free tier, so the discipline had to live in the client. The old loop had no retry, no pacing, no cache and one page per term, and a single 429 aborted every platform — at 99 search terms that would have hit Pexels' 200/hr wall on the first sweep. The cache is what makes the loop cheap to iterate on: a re-run inside the TTL costs zero requests. |

| 2026-09-16 | **Pool ranker on CLIP, behind an optional extra.** `pool.RANKERS` = `histogram` (the 2026-09-10 maths, moved behind a protocol, unchanged) and `clip` (`corpus/embed.py`: open_clip ViT-B/32 `laion2b_s34b_b79k`, fp32 on MPS, incremental fingerprinted cache in `corpus/pool/_embed/`). Score = 0.60·calibrated top-K cosine to the family's non-provisional corpus stills (K = n/8 clamped 3..8, shrunk toward the whole-corpus nearest cluster by 8 pseudo-counts, normalised by the family's own tightness and by what a wrong-family photo scores) + 0.25·aspect-class typicality + 0.15·ground/layout frequency, multiplied by gates: a measured before/after split scores 0 everywhere, and the two photo families are separated by **ratio bands** (cinematic-still ≤ 0.80, full-bleed > 0.60) rather than aspect classes. Every candidate is scored against all 15 families and allocated to the argmax when its searched family is gated out or trails by ≥ 0.10, else stays with `best_family` recorded. Sheets carry three corpus anchors and the family's genre prose; answers accept `best_family`, which moves the entry. `pool calibrate` turns the answers into a per-family auto-drop threshold at 95% recall (with a 10% deterministic exploration slice) plus per-term keep rates that decide which terms earn a second page; `pool quota` allots search volume ∝ √(n_max/n_family). | Reverses the 2026-09-10 "pHash + simple features, no CLIP" decision. That decision was right when ranking only *ordered* a human sheet pass; now the pass is agent-labelled, so review tokens — not a human hour — are the cost, and a ranker that only orders saves none of them while a calibrated one skips a third of the pile. Measured on the 2039 tagged assets: `chrome` carries 62% of family entropy but is compose chrome that never appears in a stock photo, and the only two fields the old ranker read (`ground`, `layout`) are degenerate across full-bleed (756) and cinematic-still (360) — both photo-full-bleed/single, differing only by shape. Optional extra because torch plus weights is ~1 GB and nothing in generation needs it; without it the pool still ranks on histograms and auto-drops nothing. Text-side zero-shot prior rejected: CLIP's text tower keys on subject nouns and truncates at 77 tokens, which is the wrong end of what separates these families. |

| 2026-09-16 | **Time and token efficiency of the data layer.** Built `lp-tokens` (a `tokens.py` meter that reads the run's session transcript + subagent files, dedups usage by `message.id`, bounds the manager window by the run's own spawns, and emits the `## Agents` table SKILL.md wants) and froze the live-5 baseline: manager 148 turns / $80, 21 workers 1370 turns / $281, 6 reviewers 122 turns / $29, ≈ $389 total at Opus list prices (the plan's earlier $185 was a rough estimate; cache-read at list price over 100M+ tokens dominates). Run-loop trims (procedure): bounded video polling (job handle recorded, ≤3 job_status per clip with one estimated sleep), worker `maxTurns` 80→160, reviewer drops flow.md + the ledger grep (precheck.py already reconciles credits), workers stop reading recipes.md/board.py/other runs, and `brief.py` assembles each brief from board.RECIPES + the family block + similar so the manager stops reading style-families.md/slots.json/examples in the wave. `lp-inject` writes page.md itself from each result.md; the labelling table moves to index.md. Corpus wall-clock (B1–B8): libyaml CSafeLoader/Dumper everywhere (attrs.load 4.6→0.7s, dump 2.4→0.6s), media(src)/section indexes (attrs+styles apply 10.2→0.05s), load attributes/styles once per pool search (was ~33×), lazy FrameGrabber (−0.9s/similar), attrs checkpoints to an append-only `attributes.partial.jsonl`, int-hash dedup (nearest_dup 3.5×), slim dropped pool entries, prune `_api.sqlite`. | The cost of a role is **context × turns**: cache-read is 70–85% of every bill, so a file read once is paid for on every later turn — the whole of Part A attacks turns and per-turn context, and `lp-tokens` makes both measurable. **Format decision: YAML stays the single committed form.** After the C loader the whole YAML cost of any maintenance command is ~0.8s and the two in-loop commands (`similar`, `skeleton`) read only `corpus.db`; moving attributes/styles into DB/JSON tables would save ~0.7s/command, touch ~60 dict-passing call sites, and still need today's YAML export. Deferred (Areg's call): trim the committed project CLAUDE.md Run block + memory index (A9.5, ~2–3k tokens/turn, touches the authoritative spec), run the manager from the CLI not the desktop Code tab (A9.2, one measurement decides), disconnect the three unused connectors (A9.4). The pool-file slim ships as save-time logic only; existing files slim on their next natural write, to keep the diff off the uncommitted pool work. |

| 2026-09-17 | **Massive Pexels pool enlargement (opt-in `--deep` + more terms).** Areg: "enlarge the web scrape pool pexels, massively" → **Both**. Diagnosed the plateau (~1,450 entries): `pool.search` is breadth-first and stops each term after page 1 via `deepen`'s keep-floor and `YIELD_FLOOR`, and every family's curated terms are page-1-exhausted (their admittable page-1 photos already held), so no term reaches pages 2+ where new photos live — `--pages`/`--keep-floor` alone add nothing. Verified against live Pexels: deep pages hold ~78/80 new, a term runs out ~page 6–8. Added an opt-in `--deep` flag (`cli.py` + `pool.search(deep=)`) that bypasses both early-stops so `--pages N` pages every term to full depth, keeping the natural short-page and per-term-cap stops; default unchanged; new test + 40 pool tests green. Expanded `photography.search_terms` +14/family (~7→21, 309 total) with on-genre variations via a formatting-preserving line inserter. Kicked off a background sweep (`scratchpad/pexels_sweep.sh`): all 15 families thinnest-first, `--platform pexels --deep --pages 8 --limit-per-term 200`, re-passing hourly (limiter self-caps at 180 req/hr) until a full pass adds 0 new. One deep `vs-two-up` run = 22 req → 280 admitted → 135 net-new fanned across 7 thin families. Baseline before sweep 1,734. Free-tier, zero credits/spend. UNCOMMITTED on branch `style-families` with the rest of the pool work. | The pool was built to *order* a review pass, so its defaults optimise breadth over depth and deliberately stop mining an exhausted term — which caps total intake once the curated term set is spent. "Massively" needs both levers: depth (defeat the yield-floor for the terms we have) and breadth (more terms). Kept as an opt-in flag rather than a default change so the normal curated intake still prefers relevance; the downstream threshold + agent review remain the quality gate on the extra volume. Separately noted (not fixed): the `lp-corpus fetch` scrape of the 204-page queue is broken because Picsart moved landing pages to Next.js RSC streaming — content now lives in `<script>__next_f.push>` with an empty SSR body, so `reassemble()` is obsolete; the fix is to snapshot the hydrated Playwright DOM. |

| 2026-09-19 | **Video as context and for accurate generation** (branch `video-context`, plan `~/.claude/plans/what-is-the-current-eventual-crane.md`). Diagnosis: video was first-class through snapshot → DB → skeleton → inject but reduced to ONE Chromium frame at "t=1 s" everywhere that mattered — and that frame was frame 0, because `route.fulfill(path=)` serves a local clip without byte ranges (unseekable). No motion field existed; `duration` was captured then dropped by `attrs.record`; the designer poster was downloaded and never read; `brief.py` had lost live-5's hand-written video paragraph; `board.RECIPES`/`check` had no video rule; the reviewer could not open an mp4 and paraphrased the worker's note; `lp-bench` skipped mp4s; live-5 shipped 5 s clips against 10–34 s originals unflagged and lost ~190 cr to orphaned finals the ledger could not see. Built: **P0** `job_status` hooked (URL into the ledger), `duration` kept, video recipe (`recipe_for(family, "video")` = still recipe + mini draft + final) and `video_problems` in `lp-flow check` (audio off, async, `<step N passed>` wiring, no `imageUrls`, draft before final, ≤ 30 s), `poster:`/`duration_s:` in the contract and shipped by `lp-inject` with `muted autoplay loop playsinline`; **P1** `corpus/motion.py` (five sampled frames via `FrameGrabber.grab_many` — range-aware route, seek + canvas — → `motion`, `pace`, `loop`, `loop_seam`, `camera` only when the frame holds; 3-frame strips; per-family `summary` → `**Motion:**` line), video-only `attrs.FIELDS` (`motion_kind`, `camera` labelled on video sheets grouped `(type, aspect, video)` with strip cells), `media.poster` column + `data-lp-poster` read by sectionize, `attrs` preferring the local poster (`--force` refreshes), `lp-corpus frames|motion`; **P2** `similar --kind video` (clips first, strip excerpts, motion keys in `built:`), `brief.py` `## Video` section (models, wiring, Motion line, per-slot target duration from the skeleton's new `> duration:` line, `budget.video_seconds` cap 30), `precheck.py` video checks (target duration, job_status ownership, orphan rows); **P3** `evaluation.md` keys `first_frame`/`motion`/`loop`, reviewer + worker gate on the strip with zero new tool grants, `lp-bench` video rows (`[duration]`, `[loop]`); **P4** Pexels/Pixabay video search (`stock.SEARCH_VIDEO`, `pool search --kind video`, `pick(kind)`, strips in `write_examples`, `similar --pool --kind video`); **P5** MP Scene render tools gated by the credit guard until `_ledger.RENDER_PRICE` carries a measured price. Data: corpus re-indexed (620/846 video rows carry a poster), every generated-role video re-measured (`attrs --force --kinds video`). Tests: ~215 green. Not yet: the video sheets labelled (`/label-corpus`; done since: 45/45 sheets, 434 clips answered), `**Motion:**` lines written (needs ≥ 5 labelled clips per family; since written for 8 families), one live MP Scene render to price, the `runs/live-6` trial. | Areg's ask: the state of video as context and for accuracy, then a plan. Decisions: duration faithful to the original (not a fixed loop); MP Scene pilot and a stock video pool in scope; a ≤ 200 cr trial of ~3 video slots at the end. No new dependency — the machine has no ffmpeg and Chromium already decodes both the corpus VP9 webm and Seedance's H.264 mp4 (spike). Only `static` is auto-written for `camera`: phase correlation on five 128 px frames is pulled by the subject (live-5 S03 measures 1.4–2.8 px shift on a static camera), so the rest is labelled, like chrome. The reviewer keeps zero connector grants: the strip is a file it can `Read`, which is also what makes the review independent of the worker's note. |
